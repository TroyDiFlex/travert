# Локальный репозиторий Travert

Создан 2026-09-14 из локального `income-dashboard`, коммит `92738eaa8f1c0dcc8283d3afc9b669f6842de414`.

- Рабочая ветка нового репозитория: `main`.
- Исходная точка от старого проекта сохранена тегом `finance-v2-base-2026-09-14`.
- Это независимая локальная копия Git с историей, без общих файлов объектов и без настроенного удалённого репозитория. Форк на GitHub не создавался.
- Исходный `income-dashboard` остаётся рабочей версией приложения.
- `.local/`, учётные данные clasp и настройки развёртывания не переносились.
- В текущем `config.js` рабочий адрес Apps Script заменён несуществующим адресом-заглушкой. При заглушке приложение открывается в локальном режиме: доходы хранятся в `localStorage` через `local-income.js`, вход без пароля (`local-income.js` повторяет ревизии, корзину и формат бэкапов сервера). Тесты используют имитацию запросов.
- Финансовое ядро и локальный репозиторий IndexedDB первого этапа реализованы в `finance/`. Облачный сервер не подключён: варианты, требующие карту или платёжный профиль, исключены. Новый интерфейс ещё не подключён. Старые README, интерфейс и `server/` описывают унаследованное приложение.

Рабочее имя папки нового проекта — `Travert`; смысл названия и продуктовая айдентика пока не определены. Архитектура следующего этапа: [finance-v2](docs/finance-v2.md). Подробности очереди и конфликтов: [синхронизация](docs/finance-v2-sync.md).

Чтобы пользоваться нынешним приложением, продолжайте открывать существующий сайт или исходный проект. Его данные и ежедневные резервные копии работают отдельно от этого форка.

Не возвращайте рабочий URL Apps Script в конфигурацию экспериментов. Реальные данные импортируются в finance-v2 только отдельным этапом после проверки восстановления. Публикация сайта, GitHub-репозитория и облачной конфигурации требует отдельного запроса пользователя.
