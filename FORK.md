# Локальный форк finance-v2

Создан 2026-09-14 из локального `income-dashboard`, коммит `92738eaa8f1c0dcc8283d3afc9b669f6842de414`.

- Рабочая ветка: `finance-v2`.
- Исходная точка в этом форке: ветка `main` и тег `finance-v2-base-2026-09-14`.
- Это независимая локальная копия Git с историей, без общих файлов объектов и без настроенного удалённого репозитория. Форк на GitHub не создавался.
- Исходный `income-dashboard` остаётся рабочей версией приложения.
- `.local/`, учётные данные clasp и настройки развёртывания не переносились.
- В текущем `config.js` рабочий адрес Apps Script заменён несуществующим адресом-заглушкой. Вход через старый сервер в этой копии не работает; тесты используют имитацию запросов.
- Firebase ещё не подключён. Старые README, интерфейс и `server/` описывают унаследованное приложение, а не реализованный finance-v2.

Проект следующего этапа: [finance-v2](docs/finance-v2.md). Подробности очереди и конфликтов: [синхронизация](docs/finance-v2-sync.md).

Чтобы пользоваться нынешним приложением, продолжайте открывать существующий сайт или исходный проект. Его данные и ежедневные резервные копии работают отдельно от этого форка.

Не возвращайте рабочий URL Apps Script в конфигурацию экспериментов. Реальные данные импортируются в finance-v2 только отдельным этапом после проверки восстановления. Публикация сайта, GitHub-репозитория и облачной конфигурации требует отдельного запроса пользователя.
